server:
  port: 8090  # Para que use un puerto aleatorio (microservicio escalable)

spring:
  application:
    name: ms-factura
  datasource:
    url: jdbc:mysql://localhost:3306/ms-factura
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka